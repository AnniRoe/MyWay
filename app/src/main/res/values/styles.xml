<resources>

    <!-- Base application theme. -->
    <style name="MyWay1" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:colorBackground">@color/background_material_light</item>
        <item name="colorAccent">@color/colorSecondary</item>
    </style>

    <style name="MyWay1.NoActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyWay" parent="MyWay1" >
        <item name="colorAccent">@color/colorPrimaryDark</item>
    </style>

    <style name="MyWay1.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="MyWay1.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MYWAYACTUAL" parent="MyWay" />

</resources>
